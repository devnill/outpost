# Notes: WI-026 — Documentation Sweep

## mcp/session-spawner/README.md

### token_usage (lines 73 and 228)

Search for "Omitted otherwise" — change both occurrences to reflect the always-present null contract:
- Old: "token_usage is included when the spawned session returns JSON output containing token information. Omitted otherwise."
- New: "`token_usage` is always present. It contains the token counts when the session returns JSON output with token information; it is `null` when token information is unavailable."

Similarly update line 228 (Token Budget Logging section): replace any "the field is omitted" phrasing with "the field is `null`."

### cancel_remote_job tool table

Add a row for `cancel_remote_job` to the tool table. Model the description on `poll_remote_job`:
| `cancel_remote_job` | `job_id` (required), `worker_name` (optional) | Cancels a queued or running job on a remote worker. Returns `{"status": "cancelled"}` on success or an error. |

## specs/plan/architecture.md

### Component Map tool list

Find the session-spawner row in the Component Map MCP Servers table (around line 10-20). Add `cancel_remote_job` to the Tools column.

### Health endpoint response schema

Find the GET /health response schema (search for `active_jobs` or `max_concurrency`). Add `max_jobs: integer` to the response fields.

## mcp/remote-worker/README.md

### Environment Variables table

Find the env var table (search for `IDEATE_WORKER_MAX_CONCURRENCY`). Add a row:
| IDEATE_WORKER_MAX_JOBS | Maximum number of jobs to retain in the in-memory store (terminal jobs are evicted oldest-first when the limit is exceeded) | 1000 |
