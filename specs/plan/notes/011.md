# Implementation Notes: 011 — Session Spawner Verification

## Context

Work item 010 (Session Spawner MCP Server) was completed in the ideate project before outpost was extracted as a standalone project. The implementation was migrated but never received an incremental review within the outpost artifact structure. This work item verifies the existing implementation is correct and complete.

## Approach

This is a **verify-and-fix** work item, not a greenfield implementation.

1. Read the existing implementation at `mcp/session-spawner/server.py` (1107 lines).
2. Run the existing test suite: `cd /Users/dan/code/outpost/mcp/session-spawner && pytest -v`
3. Walk each acceptance criterion. For each:
   - Check whether the implementation satisfies it
   - If unsatisfied: fix the gap in `server.py` and/or `test_server.py`
   - If intentionally different (e.g., env var naming changed from IDEATE_* to OUTPOST_*): document the deviation
4. Verify the README covers all environment variables listed in `specs/plan/architecture.md` section 8.

## Known Deviations from Original Spec

The original work item 010 spec used `IDEATE_SPAWN_DEPTH` for depth tracking. The implemented code uses `OUTPOST_SPAWN_DEPTH` and `OUTPOST_MAX_DEPTH`. This rename is intentional (project was rebranded from ideate to outpost). Do not revert; document as intentional.

The original spec included `output_format` as a parameter. The current implementation may not expose this. Check and document.

## Files to Read

- `mcp/session-spawner/server.py` — main implementation
- `mcp/session-spawner/test_server.py` — existing tests (55 functions)
- `mcp/session-spawner/requirements.txt` — declared dependencies
- `mcp/session-spawner/README.md` — current documentation
- `specs/plan/architecture.md` — tool definitions (section 3) and config (section 8)
- `specs/plan/work-items-legacy/010-session-spawner-mcp.md` — original spec for reference

## Test Execution

```bash
cd /Users/dan/code/outpost/mcp/session-spawner
pip install -r requirements.txt
pytest -v
```

If tests fail, fix the implementation before reporting completion.
