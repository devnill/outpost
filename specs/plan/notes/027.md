# Notes: WI-027 — Minor Code Hygiene

## Add __init__.py files

Create empty `mcp/remote-worker/__init__.py` and `mcp/session-spawner/__init__.py`. This makes each directory a Python package and resolves the pytest import collision when running `pytest mcp/` from the project root (both directories had `test_server.py` files, causing a name clash).

After adding these files, verify `pytest mcp/` discovers and runs all test files without import errors.

## _evict_terminal_jobs_locked comment

In `mcp/remote-worker/server.py`, add a comment to `_evict_terminal_jobs_locked`:

```python
def _evict_terminal_jobs_locked() -> None:
    # Must be called while holding job_store_lock.
    """Remove oldest terminal jobs when job_store exceeds _max_jobs."""
    ...
```

## worker_server fixture _max_jobs reset

In `mcp/test_integration.py`, in the `worker_server` fixture teardown (after `server.should_exit = True` and `await task`), add:

```python
worker_mod._max_jobs = 1000  # Reset to default to avoid cross-test pollution
```
