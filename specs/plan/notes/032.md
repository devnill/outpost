# WI-032: Documentation sweep (cycle 7 minor gaps)

## Context

Four documentation gaps identified in cycle 7 capstone review. All are isolated, documentation-only changes (except OQ-022 which is handled in WI-031). No code changes.

## Files to Modify

1. `README.md` (repository root) — two additions:
   - Add `cancel_remote_job` to the tool list
   - Add config cross-reference to component READMEs (or list key env vars)

2. `CLAUDE.md` (repository root) — one fix:
   - Correct the `pip install` command path

3. `specs/plan/architecture.md` — one addition:
   - Add `IDEATE_WORKER_MAX_JOBS` row to Section 8 env var table

## Fix Details

### README.md — cancel_remote_job (OQ-027)

In the Usage section, find the tool list. It currently lists four tools. Add `cancel_remote_job` as the fifth. Preserve the existing list format (bullet points or table, whichever is used).

### README.md — config cross-reference (OQ-028)

In the Configuration section, after the `OUTPOST_REMOTE_WORKERS` entry (currently the only documented variable), add a note directing users to the component READMEs for the complete list:

```
For the full list of environment variables, see:
- `mcp/session-spawner/README.md` — session-spawner configuration
- `mcp/remote-worker/README.md` — remote-worker configuration
```

Alternatively, reproduce at minimum `OUTPOST_SAFE_ROOT` (security boundary) and `OUTPOST_LOG_FILE` (observability) if a cross-reference feels too sparse.

### CLAUDE.md — requirements.txt path (OQ-026)

Find the Installation section. Change:
```
pip install -r requirements.txt
```
To:
```
pip install -r mcp/session-spawner/requirements.txt
```

If the remote-worker also needs to be installed, add:
```
pip install -r mcp/remote-worker/requirements.txt
```

Only change the requirements.txt path — do not restructure the rest of the installation instructions.

### specs/plan/architecture.md — IDEATE_WORKER_MAX_JOBS (OQ-020)

Find Section 8 (Configuration / Environment Variables). Find the table of environment variables. Add a new row:

| Variable | Component | Default | Description |
|---|---|---|---|
| `IDEATE_WORKER_MAX_JOBS` | remote-worker | 1000 | Maximum number of job records retained in the in-memory job store before LRU eviction of oldest terminal jobs |

Insert it near the other `IDEATE_WORKER_*` entries for readability.

## Verification

- All four changes are present
- No other content in the modified files is altered
- Run `pytest mcp/` to confirm no tests are broken (documentation-only changes should have no effect on tests)
