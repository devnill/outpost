# Notes: WI-023 — README and Architecture Documentation Fixes

## Files to Update

### specs/plan/architecture.md

1. **Job States table** (around line 223):
   Change: `| cancelled | Cancelled while queued |`
   To: `| cancelled | Cancelled while queued or running |`

2. **Configuration env vars table** (around line 304):
   The `OUTPOST_TIMEOUT` row. Either:
   - Remove it entirely, OR
   - Change Purpose to: `Default session timeout — not implemented; timeout is a per-call parameter only`

   Recommendation: keep the row but annotate it as "not implemented" so operators who look for it understand why setting it has no effect.

3. **Add IDEATE_WORKER_HOST**:
   Add a new row to the remote-worker section of the env vars table:
   `| IDEATE_WORKER_HOST | remote-worker | Listen host address | 0.0.0.0 |`

### mcp/remote-worker/README.md

Search for:
- "Only jobs with status `queued` can be cancelled" → change to "Jobs with status `queued` or `running` can be cancelled"
- The 409 error condition about "job is not in `queued` status" → change to reflect completed/failed/cancelled are non-cancellable
- Job lifecycle diagram (if present) — add `running → cancelled` transition

For role description:
- Find any text saying role is "an observability label" or that constraints are "not applied" → update to accurately state that `allowed_tools`, `permission_mode`, `max_turns`, and `system_prompt` are all propagated from the role

### mcp/session-spawner/README.md

Find any text saying role is "observability label only" for remote dispatch → update to accurately describe role behavior post-WI-015 and WI-018.

## No test needed

Documentation-only change. Verify by reading the updated files.
